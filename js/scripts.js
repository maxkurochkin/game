/* =================== */
/* === Game Script === */
/* =================== */
function script(script) {
	var script = script.split('.');
	if (script[0] == 'item') {
		if (script[1] == 'add') {

		}
	}
}